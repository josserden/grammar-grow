import React from "react";

import { Typography } from "@/shared/components/ui/Typography";
import { Wrapper } from "@/shared/components/ui/Wrapper";

const Play = () => {
  return (
    <Wrapper>
      <Typography variant="title">Play</Typography>
    </Wrapper>
  );
};

export default Play;
